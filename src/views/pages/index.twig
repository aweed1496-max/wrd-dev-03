{#
| Variable         | Type       | Description                                                         |
|------------------|------------|---------------------------------------------------------------------|
| page             | object     |                                                                     |
| page.title       | string     | *could be html                                                      |
| page.slug        | string     | ex: "cat.show"                                                      |
#}
{% extends "layouts.master" %}
{% block content %}
    <div class="home-theme">
        <section class="relative overflow-hidden" style="background: linear-gradient(180deg, var(--home-alt-bg), var(--home-bg));">
            <div class="absolute -top-24 -right-24 h-96 w-96 rounded-full bg-gradient-to-br from-violet-300/40 via-purple-200/40 to-blue-200/10 blur-[140px]"></div>
            <div class="absolute bottom-0 -left-32 h-[420px] w-[420px] rounded-full bg-gradient-to-br from-blue-200/30 via-cyan-200/20 to-transparent blur-[160px]"></div>
            <div class="container relative z-10 py-20 lg:py-28">
                <div class="grid items-center gap-14 lg:grid-cols-2">
                    <div>
                        <span class="inline-flex items-center gap-2 rounded-full border border-transparent bg-white px-4 py-2 text-sm home-text-muted shadow-[6px_6px_14px_var(--home-shadow-dark),-6px_-6px_14px_var(--home-shadow-light)]">
                            استوديو جرافيك وهويات بصرية
                        </span>
                        <h1 class="mt-6 text-4xl font-semibold leading-tight sm:text-5xl lg:text-6xl">
                            جرافيك يصنع حضورك البصري
                        </h1>
                        <p class="mt-5 max-w-xl text-lg home-text-muted">
                            نقدم حلول هوية بصرية، جرافيك احترافي، موشن جرافيك، مونتاج، وتصميم ثلاثي الأبعاد بأسلوب عصري فاخر.
                        </p>
                        <div class="mt-8 flex flex-wrap gap-4">
                            <a href="#pricing" class="rounded-xl bg-white px-6 py-3 text-sm font-semibold text-[#0b102b] shadow-lg shadow-white/20 transition hover:-translate-y-0.5">
                                ابدأ الآن
                            </a>
                            <a href="#services" class="rounded-xl border border-transparent bg-white px-6 py-3 text-sm font-semibold text-slate-700 shadow-[6px_6px_14px_var(--home-shadow-dark),-6px_-6px_14px_var(--home-shadow-light)] transition hover:-translate-y-0.5">
                                استعرض الخدمات
                            </a>
                        </div>
                        <div class="mt-10 grid gap-4 sm:grid-cols-3">
                            <div class="home-neu-card p-4">
                                <p class="text-2xl font-semibold">+50K</p>
                                <p class="text-sm home-text-muted">عميل عالمي</p>
                            </div>
                            <div class="home-neu-card p-4">
                                <p class="text-2xl font-semibold">+12K</p>
                                <p class="text-sm home-text-muted">مشروع ناجح</p>
                            </div>
                            <div class="home-neu-card p-4">
                                <p class="text-2xl font-semibold">24/7</p>
                                <p class="text-sm home-text-muted">دعم احترافي</p>
                            </div>
                        </div>
                    </div>
                    <div class="relative">
                        <div class="absolute -top-10 right-10 h-64 w-64 rounded-full bg-gradient-to-br from-violet-200/70 via-purple-200/50 to-blue-200/30 blur-3xl"></div>
                        <div class="relative rounded-[32px] bg-white p-8 shadow-[20px_20px_40px_var(--home-shadow-dark),-20px_-20px_40px_var(--home-shadow-light)]">
                            <div class="flex items-center justify-between">
                                <span class="text-sm home-text-muted">لوحة عرض إبداعية</span>
                                <span class="rounded-full bg-white px-3 py-1 text-xs home-text-muted shadow-[4px_4px_10px_var(--home-shadow-dark),-4px_-4px_10px_var(--home-shadow-light)]">Graphic Studio</span>
                            </div>
                            <div class="mt-6 space-y-4">
                                <div class="h-32 rounded-2xl bg-gradient-to-br from-slate-200 via-slate-100 to-transparent"></div>
                                <div class="grid grid-cols-2 gap-4">
                                    <div class="h-20 rounded-2xl bg-slate-100"></div>
                                    <div class="h-20 rounded-2xl bg-slate-100"></div>
                                </div>
                            </div>
                            <div class="mt-6 flex items-center gap-4 rounded-2xl bg-white p-4 shadow-[6px_6px_14px_var(--home-shadow-dark),-6px_-6px_14px_var(--home-shadow-light)]">
                                <div class="h-12 w-12 rounded-xl bg-gradient-to-br from-violet-500 to-blue-500"></div>
                                <div>
                                    <p class="text-sm font-semibold">حضور بصري فاخر</p>
                                    <p class="text-xs home-text-muted">تصاميم ترفع قيمة العلامة</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <section id="services" class="py-20" style="background: var(--home-alt-bg);">
            <div class="container">
                <div class="max-w-2xl">
                    <p class="text-sm font-semibold text-violet-600">خدماتنا</p>
                    <h2 class="mt-3 text-3xl font-semibold sm:text-4xl">حلول جرافيك وهوية متكاملة</h2>
                    <p class="mt-4 home-text-muted">كل ما تحتاجه لبناء حضور بصري فخم ومؤثر.</p>
                </div>
                <div class="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">
                    {% set services = [
                        {id: 'service-branding', title: 'خدمات تصميم العلامة', price: '100', desc: 'هوية بصرية متكاملة تشمل الشعار والألوان والتطبيقات.'},
                        {id: 'service-graphic', title: 'تصميم جرافيك', desc: 'تصاميم احترافية للسوشيال والمطبوعات والإعلانات.'},
                        {id: 'service-motion', title: 'موشن جرافيك', desc: 'حركات ترويجية تعكس قيمة علامتك بوضوح.'},
                        {id: 'service-pitch', title: 'دورة العرض الاحترافي', price: '255', desc: 'منتج رقمي احترافي لتطوير العروض التقديمية.'}
                    ] %}
                    {% for service in services %}
                        <div id="{{ service.id }}" class="group home-neu-card p-6 transition hover:-translate-y-1">
                            <div class="mb-6 h-12 w-12 rounded-2xl bg-gradient-to-br from-violet-500 via-purple-500 to-blue-500"></div>
                            <h3 class="text-lg font-semibold">{{ service.title }}</h3>
                            <p class="mt-3 text-sm home-text-muted">{{ service.desc }}</p>
                            {% if service.price %}
                                <div class="mt-5 flex flex-wrap items-center gap-3">
                                    <span class="inline-flex items-center gap-2 rounded-full bg-white px-3 py-1 text-xs font-semibold home-text-muted shadow-[4px_4px_10px_var(--home-shadow-dark),-4px_-4px_10px_var(--home-shadow-light)]">
                                        {{ service.price }} ريال
                                    </span>
                                    <a href="#pricing" class="rounded-lg bg-slate-900 px-4 py-2 text-xs font-semibold text-white transition hover:-translate-y-0.5">
                                        شراء الآن
                                    </a>
                                </div>
                            {% endif %}
                        </div>
                    {% endfor %}
                </div>
            </div>
        </section>

        <section id="portfolio" class="py-20" style="background: var(--home-bg);">
            <div class="container">
                {% set enable_logo_ticker = theme.settings.get('enable_logo_ticker', true) %}
                {% set logo_ticker_title = theme.settings.get('logo_ticker_title', 'شعارات صممناها بعناية') %}
                {% set logo_ticker_description = theme.settings.get('logo_ticker_description', 'شريط متحرك يعرض أبرز الهويات البصرية التي نفذناها.') %}
                {% set logo_items = theme.settings.get('logo_ticker_items', []) %}
                {% if logo_items is empty %}
                    {% set logo_items = [
                        {'logo.name': 'أريج'}, {'logo.name': 'لمعة'}, {'logo.name': 'سُكرة'},
                        {'logo.name': 'مِداد'}, {'logo.name': 'أصالة'}, {'logo.name': 'زُهرة'},
                        {'logo.name': 'أفق'}, {'logo.name': 'نور'}, {'logo.name': 'رواء'}, {'logo.name': 'أصيل'}
                    ] %}
                {% endif %}
                {% if enable_logo_ticker %}
                    <div class="mb-10 grid gap-6 lg:grid-cols-[1fr_auto] lg:items-center">
                        <div>
                            <p class="text-sm font-semibold text-violet-600">علامات وثقت بنا</p>
                            <h2 class="mt-3 text-2xl font-semibold sm:text-3xl">{{ logo_ticker_title }}</h2>
                            <p class="mt-3 text-slate-600">{{ logo_ticker_description }}</p>
                        </div>
                        <div class="relative overflow-hidden rounded-3xl bg-[#f3f1ec] px-6 py-4 shadow-[10px_10px_24px_#d9d6cf,-10px_-10px_24px_#ffffff]">
                            <div class="pointer-events-none absolute inset-0 bg-gradient-to-r from-[#f3f1ec] via-transparent to-[#f3f1ec]"></div>
                            <div class="flex items-center gap-6 whitespace-nowrap" style="animation: marquee-rtl 18s linear infinite;">
                                {% for logo in logo_items %}
                                    {% set logo_name = logo['logo.name']|default(logo.name|default('')) %}
                                    {% set logo_image = logo['logo.image']|default(logo.image|default('')) %}
                                    {% set logo_url = logo['logo.url']|default(logo.url|default('')) %}
                                    {% set logo_content %}
                                        {% if logo_image %}
                                            <img src="{{ logo_image }}" alt="{{ logo_name }}" class="h-8 w-auto max-w-[96px] object-contain" loading="lazy" />
                                        {% else %}
                                            {{ logo_name }}
                                        {% endif %}
                                    {% endset %}
                                    <div class="flex h-12 w-28 items-center justify-center rounded-2xl bg-[#f3f1ec] text-sm font-semibold text-slate-600 shadow-[6px_6px_14px_#d9d6cf,-6px_-6px_14px_#ffffff]">
                                        {% if logo_url %}
                                            <a href="{{ logo_url }}" class="flex h-full w-full items-center justify-center">{{ logo_content }}</a>
                                        {% else %}
                                            {{ logo_content }}
                                        {% endif %}
                                    </div>
                                {% endfor %}
                            </div>
                        </div>
                    </div>
                {% endif %}
                <div class="flex flex-wrap items-end justify-between gap-6">
                    <div>
                        <p class="text-sm font-semibold text-violet-600">الأعمال</p>
                            {% set portfolio_title = theme.settings.get('portfolio_section_title', 'مختارات من أعمال الجرافيك') %}
                            <h2 class="mt-3 text-3xl font-semibold sm:text-4xl">{{ portfolio_title }}</h2>
                            {% set portfolio_description = theme.settings.get('portfolio_section_description', '') %}
                            {% if portfolio_description %}
                                <p class="mt-3 home-text-muted">{{ portfolio_description }}</p>
                            {% endif %}
                    </div>
                    <a href="#" class="rounded-xl border border-slate-200 bg-white px-5 py-2 text-sm font-semibold text-slate-600 shadow-sm transition hover:border-slate-300">عرض الكل</a>
                </div>
                <div class="mt-10 grid gap-6 md:grid-cols-2 lg:grid-cols-3">
                        {% set portfolio_items = theme.settings.get('portfolio_items', []) %}
                        {% if portfolio_items is empty %}
                            {% set portfolio_items = [
                                {
                                    'portfolio.category': 'خدمات تصميم العلامة',
                                    'portfolio.title': 'هوية بصرية فاخرة',
                                    'portfolio.description': 'شعار، ألوان، خطوط، وتطبيقات بصرية متناغمة.',
                                    'portfolio.image': 'images/placeholder.png',
                                    'portfolio.url': ''
                                }
                            ] %}
                        {% endif %}
                        {% for item in portfolio_items %}
                            {% set item_category = item['portfolio.category']|default(item.category|default('')) %}
                            {% set item_title = item['portfolio.title']|default(item.title|default('')) %}
                            {% set item_description = item['portfolio.description']|default(item.description|default('')) %}
                            {% set item_image = item['portfolio.image']|default(item.image|default('images/placeholder.png')) %}
                            {% set item_url = item['portfolio.url']|default(item.url|default('')) %}
                        <article class="group home-neu-card p-5 transition hover:-translate-y-1">
                            <div class="overflow-hidden rounded-2xl">
                                    <img class="h-52 w-full object-cover transition duration-500 group-hover:scale-105" src="{{ item_image | asset }}" alt="{{ item_title ?: 'عمل' }}">
                            </div>
                            <div class="mt-5">
                                    {% if item_category %}
                                        <span class="text-xs font-semibold text-violet-600">{{ item_category }}</span>
                                    {% endif %}
                                    {% if item_title %}
                                        <h3 class="mt-2 text-lg font-semibold">{{ item_title }}</h3>
                                    {% endif %}
                                    {% if item_description %}
                                        <p class="mt-2 text-sm home-text-muted">{{ item_description }}</p>
                                    {% endif %}
                                    {% if item_url %}
                                        <a href="{{ item_url }}" class="mt-4 inline-flex text-sm font-semibold text-slate-900">عرض التفاصيل</a>
                                    {% endif %}
                            </div>
                        </article>
                    {% endfor %}
                </div>
            </div>
        </section>

        <section id="features" class="py-20" style="background: var(--home-alt-bg);">
            <div class="container">
                <div class="max-w-2xl">
                    <p class="text-sm font-semibold text-violet-600">المميزات</p>
                    <h2 class="mt-3 text-3xl font-semibold sm:text-4xl">لماذا تختار فريقنا؟</h2>
                </div>
                <div class="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
                    {% set features = [
                        {title: 'سرعة التنفيذ', desc: 'تسليم سريع مع التزام كامل بالمواعيد.'},
                        {title: 'جودة عالية', desc: 'تفاصيل دقيقة وإخراج فخم للتصاميم.'},
                        {title: 'إبداع بصري', desc: 'أفكار مبتكرة تعكس هوية العلامة.'},
                        {title: 'دعم مستمر', desc: 'متابعة مستمرة حتى بعد التسليم.'},
                        {title: 'اتساق الهوية', desc: 'لغة بصرية موحدة في كل نقطة تواصل.'},
                        {title: 'نتائج مؤثرة', desc: 'تصاميم ترفع الانطباع وتزيد التفاعل.'}
                    ] %}
                    {% for feature in features %}
                        <div class="home-neu-card p-6 transition hover:-translate-y-1">
                            <div class="mb-4 h-10 w-10 rounded-xl bg-gradient-to-br from-violet-500 to-blue-500"></div>
                            <h3 class="text-lg font-semibold">{{ feature.title }}</h3>
                            <p class="mt-3 text-sm home-text-muted">{{ feature.desc }}</p>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </section>

        <section id="testimonials" class="py-20" style="background: var(--home-bg);">
            <div class="container">
                <div class="flex flex-wrap items-center justify-between gap-6">
                    <div>
                        <p class="text-sm font-semibold text-violet-600">آراء العملاء</p>
                        <h2 class="mt-3 text-3xl font-semibold sm:text-4xl">ثقة العملاء هي قوتنا</h2>
                    </div>
                    <div class="flex items-center gap-2 text-sm home-text-muted">
                        <span class="h-2 w-2 rounded-full bg-violet-400"></span>
                        تقييم 4.9 من 5
                    </div>
                </div>
                <div class="mt-10 grid gap-6 md:grid-cols-2 lg:grid-cols-3">
                    {% set testimonials = [
                        {name: 'سارة العتيبي', role: 'مديرة علامة تجارية', text: 'هوية بصرية متكاملة رفعت من حضور العلامة.'},
                        {name: 'عبدالله الزهراني', role: 'مالك متجر', text: 'تصاميم جرافيك احترافية للسوشيال والمطبوعات.'},
                        {name: 'نورة الغامدي', role: 'مديرة تسويق', text: 'موشن جرافيك ومونتاج بجودة عالية ودعم سريع.'}
                    ] %}
                    {% for item in testimonials %}
                        <div class="home-neu-card p-6">
                            <div class="flex items-center gap-4">
                                <div class="h-12 w-12 rounded-full bg-gradient-to-br from-violet-500 to-blue-500"></div>
                                <div>
                                    <p class="text-base font-semibold">{{ item.name }}</p>
                                    <p class="text-xs home-text-muted">{{ item.role }}</p>
                                </div>
                            </div>
                            <p class="mt-4 text-sm home-text-muted">"{{ item.text }}"</p>
                            <div class="mt-4 text-sm text-violet-600">★★★★★</div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </section>

        <section id="pricing" class="py-20" style="background: var(--home-alt-bg);">
            <div class="container">
                <div class="max-w-2xl">
                    <p class="text-sm font-semibold text-violet-600">الباقات</p>
                    <h2 class="mt-3 text-3xl font-semibold sm:text-4xl">باقات جرافيك تناسب احتياجك</h2>
                    <p class="mt-4 home-text-muted">اختر الباقة المناسبة لهويتك ومحتواك البصري.</p>
                </div>
                <div class="mt-10 grid gap-6 lg:grid-cols-3">
                    {% set plans = [
                        {name: 'الأساسية', price: '2,900', desc: 'مثالية للهوية والمحتوى الأساسي.', featured: false},
                        {name: 'الاحترافية', price: '7,500', desc: 'حزمة جرافيك شاملة مع موشن.', featured: true},
                        {name: 'المؤسسات', price: '14,500', desc: 'حل بصري متكامل للعلامات الكبرى.', featured: false}
                    ] %}
                    {% for plan in plans %}
                        <div class="relative home-neu-card p-6 transition hover:-translate-y-1 {% if plan.featured %}ring-2 ring-violet-300 shadow-[0_20px_50px_rgba(140,120,255,0.18)]{% endif %}">
                            {% if plan.featured %}
                                <span class="absolute -top-3 right-6 rounded-full bg-gradient-to-r from-violet-500 to-blue-500 px-4 py-1 text-xs font-semibold text-white">الأكثر طلباً</span>
                            {% endif %}
                            <h3 class="text-xl font-semibold">{{ plan.name }}</h3>
                            <p class="mt-2 text-sm home-text-muted">{{ plan.desc }}</p>
                            <div class="mt-6 flex items-end gap-2">
                                <span class="text-3xl font-semibold">{{ plan.price }}</span>
                                <span class="text-sm home-text-muted">ريال</span>
                            </div>
                            <ul class="mt-6 space-y-3 text-sm home-text-muted">
                                <li>جلسات استراتيجية</li>
                                <li>تصاميم سوشيال ومطبوعات</li>
                                <li>تسليم ملفات قابلة للتوسع</li>
                                <li>دعم لمدة 30 يوم</li>
                            </ul>
                            <a href="#" class="mt-8 inline-flex w-full justify-center rounded-xl bg-slate-900 px-5 py-3 text-sm font-semibold text-white transition hover:-translate-y-0.5">احجز الباقة</a>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </section>

        <section id="cta" class="pb-24" style="background: var(--home-bg);">
            <div class="container">
                <div class="relative overflow-hidden rounded-[32px] bg-white p-10 shadow-[20px_20px_40px_var(--home-shadow-dark),-20px_-20px_40px_var(--home-shadow-light)]">
                    <div class="absolute -top-16 left-10 h-40 w-40 rounded-full bg-violet-300/40 blur-3xl"></div>
                    <div class="relative z-10 flex flex-col items-start gap-6 lg:flex-row lg:items-center lg:justify-between">
                        <div>
                            <h3 class="text-3xl font-semibold">جاهز لرفع مستوى الهوية البصرية؟</h3>
                            <p class="mt-3 home-text-muted">دعنا نصنع حضوراً بصرياً يرسخ في ذهن عملائك.</p>
                        </div>
                        <div class="flex flex-wrap gap-4">
                            <a href="#" class="rounded-xl bg-slate-900 px-6 py-3 text-sm font-semibold text-white shadow-lg shadow-slate-300/40">ابدأ مشروعك</a>
                            <a href="#portfolio" class="rounded-xl border border-slate-200 bg-white px-6 py-3 text-sm font-semibold text-slate-700 shadow-sm">شاهد الأعمال</a>
                        </div>
                    </div>
                </div>
            </div>
        </section>
        <style>
            @keyframes marquee-rtl {
                0% {
                    transform: translateX(0);
                }
                100% {
                    transform: translateX(-50%);
                }
            }
        </style>
    </div>
{% endblock %}
{% block scripts %}
    <script defer src="{{ 'home.js' | asset }}"></script>
{% endblock %}